# Core runtime dependencies — everything the pipeline and dashboard import.
# Lower bounds are the versions verified in the project .venv.
# Optional/future-phase packages live in requirements-optional.txt.

pandas>=2.0,<4
numpy>=1.24
scipy>=1.11
scikit-learn>=1.3
joblib>=1.3
xgboost>=2.0
lightgbm>=4.0
pyyaml>=6.0
matplotlib>=3.6
shap>=0.44
prophet>=1.1
statsmodels>=0.14
streamlit>=1.28
pytest>=7.4
