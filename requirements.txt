# Core runtime dependencies — everything Phases 0-5 actually import.
# Lower bounds are the versions verified in the project .venv.
# Optional/future-phase packages live in requirements-optional.txt.

pandas>=2.0,<4
numpy>=1.24,<2
scipy>=1.11
scikit-learn>=1.3
joblib>=1.3
xgboost>=2.0
lightgbm>=4.0
pyyaml>=6.0
matplotlib>=3.6
streamlit>=1.28
pytest>=7.4
