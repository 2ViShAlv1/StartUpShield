# Phase 0 — Repo Scaffold

## What We Have Done

- Created the project folder structure from the master spec.
- Initialized the local Git repository.
- Added starter files:
  - `README.md`
  - `requirements.txt`
  - `.gitignore`
  - `PROGRESS.md`
  - `DECISIONS.md`
  - `config/config.yaml`
- Added placeholder files for `src/`, `tests/`, `reports/`, `notebooks/`, and Streamlit app pages.
- Created the first Git commit: `phase-0: repo scaffold`.

## What You Should Study

- Basic Git commands: `git status`, `git add`, `git commit`, `git log`.
- Project folder structure and why code, data, tests, reports, and app files are separated.
- What `.gitignore` does.
- Why config values live in `config/config.yaml`.
- Difference between source code files, notebooks, reports, and generated data.

## Useful Files

- `StartupShield_AI_MASTER_BUILD_SPEC.md`
- `README.md`
- `PROGRESS.md`
- `DECISIONS.md`
- `config/config.yaml`

