# Optional dependencies for later phases and skipped alternatives.
# NOT required to run the pipeline, dashboard, or test suite. Install with:
#   pip install -r requirements-optional.txt

# Phase 9 — richer dashboard charts
plotly>=5.17

# Phase 11 (optional) — API layer
fastapi>=0.104
uvicorn>=0.24
sqlalchemy>=2.0

# Dataset sourcing — only needed to re-download the original Kaggle/HF datasets.
# Not installed: see DECISIONS.md "Phase 1 — Dataset Fallbacks".
kagglehub>=0.2
datasets>=2.14

# Skipped alternatives, kept for reference.
# DistilBERT fine-tuning was evaluated and skipped (DECISIONS.md "Phase 4").
# The Phase 6 LSTM stretch goal was skipped (DECISIONS.md "Phase 6").
# SMOTE was not used; balanced class weights handle the imbalance instead.
transformers>=4.35
torch>=2.1
imbalanced-learn>=0.11
